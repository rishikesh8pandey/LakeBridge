================================================================
        MEDALLION DATA PLATFORM - ARCHITECTURE OVERVIEW
================================================================

┌─────────────────────────────────────────────────────────────┐
│                    DATABRICKS PLATFORM                       │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  PARENT COMPANY                      │   │
│  │                                                     │   │
│  │           ┌──────────────────────┐                 │   │
│  │           │  Gold Analytics Table │                 │   │
│  │           │     (Parent ORG)      │                 │   │
│  │           └──────────┬───────────┘                 │   │
│  │                      │ Merge with                   │   │
│  └──────────────────────│─────────────────────────────┘   │
│                         │ parent gold                       │
│  ┌──────────────────────│─────────────────────────────┐   │
│  │           CHILD COMPANY        │                    │   │
│  │                                │                    │   │
│  │  ┌──────────┐  ┌───────────────────────────────┐  │   │
│  │  │          │  │        MEDALLION LAYERS         │  │   │
│  │  │Lakeflow  │  │                                 │  │   │
│  │  │  Jobs   ─┼─▶│  Bronze  ──▶  Silver  ──▶  Gold─┼──┘  │
│  │  │          │  │  Layer       Layer       Layer  │  │   │
│  │  │          │  │ (Raw Load) (Transform) (Aggreg) │  │   │
│  │  └────┬─────┘  └───────────────────────────────┘  │   │
│  │       │                                             │   │
│  └───────│─────────────────────────────────────────────┘   │
│          │                                                   │
└──────────│───────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────┐
│   S3 STORAGE     │
│                  │
│  ┌────────────┐  │
│  │  RAW Data  │  │
│  └────────────┘  │
│  ┌────────────┐  │
│  │  Archive   │  │◀──── Processed data written back
│  └────────────┘  │
└──────────────────┘

================================================================
                      SERVING LAYER
================================================================

           Gold Analytics Table (Parent ORG)
                        │
                        ▼
               ┌─────────────────┐
               │   DASHBOARDS    │
               │                 │
               │  - BI Reports   │
               │  - Visuals      │
               │  - KPIs         │
               └─────────────────┘

================================================================
                     DATA FLOW SUMMARY
================================================================

  S3 Raw Data
      │
      ▼
  Lakeflow Jobs  ──────────────────▶  S3 Archive
      │
      ▼
  Bronze Layer   (Raw ingestion, no transformation)
      │
      ▼
  Silver Layer   (Cleansed, deduplicated, typed)
      │
      ▼
  Gold Layer     (Aggregated, business-ready)  [Child ORG]
      │
      ▼  (Merge into Parent ORG)
  Gold Analytics Table                         [Parent ORG]
      │
      └──▶  Dashboards  (BI / Reporting)

================================================================
                      TECH STACK SUMMARY
================================================================

  Storage        : AWS S3
  Compute        : Databricks (Apache Spark)
  Orchestration  : Lakeflow Jobs
  Table Format   : Delta Lake
  Serving        : Databricks SQL Dashboards
  Architecture   : Medallion (Bronze → Silver → Gold)
  Pattern        : Multi-Tenant (Child → Parent merge)

================================================================
